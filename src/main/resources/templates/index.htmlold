<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Index</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
<div class="container mx-auto p-4">
    <div th:if="${#authorization.expression('isAuthenticated()')}">
        <h1 class="text-2xl font-bold">Welcome, <span th:text="${#authentication.name}"></span>!</h1>
        <p>You are logged in.</p>
        <!-- Add your application content here -->
    </div>

    <div th:unless="${#authorization.expression('isAuthenticated()')}">
        <h1 class="text-2xl font-bold">You are not logged in.</h1>
        <p>Please <a href="/login" class="text-blue-500 underline">login</a> to access this page.</p>
    </div>
</div>
</body>
</html>
